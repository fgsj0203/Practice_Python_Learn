"""
Perímetro do retângulo
Leia a base e a altura de um retângulo. Calcule seu perímetro (2 * (base + altura)).
"""

# Variables
base_value = float(input("Base: "))
height_value = float(input("Height: "))

# Printing value
print(f"The perimeter of rectangle: {(2 * (base_value + height_value))}")